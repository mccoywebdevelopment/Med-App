TODO:

OPTIMIZATIONS:
-check if user fields is modified when fetchUpdateUser
-pages (redo models/algorithms)
-send reset password even when user is not yet authenticated.

ERRORS:
-Guardian is not updating it create a new one when you add user to group,
 delete user, create user with same email, and then add user to the prev groups.
